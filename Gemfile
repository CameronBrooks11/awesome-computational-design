source "https://rubygems.org"

# Pins the exact Jekyll version and plugin set GitHub Pages runs, so a local
# build matches the deployed site. Do not install jekyll globally instead.
gem "github-pages", group: :jekyll_plugins
